version: 2.1

jobs:
  build-capstone:
    docker:
      - image: python:3.9
    steps:
      - checkout
      - restore_cache:
          keys: [fastapi-build]
      - run:
          name: Instal fastapi
          command: |          
            ls -l
            make install
workflows:
  default:
      jobs:
        - build-capstone:
            filters:
                  branches: 
                    only: main